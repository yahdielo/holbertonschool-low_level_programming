#include <stdio.h>
#include <stdlib.h>
#include <time.h>
/**
 * main - print numbers from 0 to 10
 * Return: Always 0 (Success)
 */
int main(void)
{
	int a;

	for (a = 0; a <= 9; a++)
	{
	printf("%d", a);
	}
	putchar('\n');

	return (0);
}
