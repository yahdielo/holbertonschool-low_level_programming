#include <stdio.h>
#include <stdlib.h>
#include <time.h>
/**
 * main - print number from 0 to 10
 * Return: Always 0 (Success)
 */
int main(void)
{
	int a = 0;

	while (a <= 9)
	{
	putchar(a);
	a++;
	}
	putchar('\n');

	return ('0');
}
