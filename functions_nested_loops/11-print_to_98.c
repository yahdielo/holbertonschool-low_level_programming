#include "main.h"
/**
 * print_to_98 - Print number to 98
 * Return: Always 0 (Success)
 */
void print_to_98(int i)
{
	for (i >= 98)
	{
		for (n > 98; i--)
			_putchar(n);
		_putchar(n);
	}

	for (n < 98; i++)
	{
			_putchar(n);
		_putchar(n);
	}
}
